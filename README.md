# Safe-Division-Calculator-with-Null-Safety
safe division function that:  Takes two numbers as input.  If the denominator is null or zero, it returns "Cannot divide by zero!".  Otherwise, it returns the quotient
